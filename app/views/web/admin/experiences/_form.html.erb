<<<<<<< HEAD
<%= form_with model: [:admin, @experience], local: true do |f| %>
=======
<%= form_with model: [:admin, @experience], local: true, data: (@experience.persisted? ? { confirm_message: 'Are you sure do you want to save data?', confirm_action: 'Save', confirm_skip_unchanged: 'true' } : {}) do |f| %>
>>>>>>> origin/develop
  <% if @experience.errors.any? %>
    <div class="card form-errors">
      <p>Please fix the following errors:</p>
      <ul>
        <% @experience.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-grid">
    <div>
      <%= f.label :company %>
      <%= f.text_field :company, class: 'input', required: true %>
    </div>

    <div>
      <%= f.label :role %>
      <%= f.text_field :role, class: 'input', required: true %>
    </div>

    <div>
      <%= f.label :location %>
      <%= f.text_field :location, class: 'input' %>
    </div>

    <div>
      <%= f.label :is_remote %>
      <div class="checkbox-field">
        <%= f.check_box :is_remote %>
        <%= f.label :is_remote, 'Remote role' %>
      </div>
    </div>

    <div>
      <%= f.label :start_date %>
      <%= f.date_field :start_date, class: 'input' %>
    </div>

    <div>
      <%= f.label :end_date %>
      <%= f.date_field :end_date, class: 'input' %>
    </div>
<<<<<<< HEAD

    <div class="full">
      <%= f.label :commit_hash %>
      <%= f.text_field :commit_hash, class: 'input' %>
      <p class="small">Optional commit hash reference for reproducible snapshots</p>
    </div>
  </div>

  <% if @experience.experience_highlights.any? %>
    <div class="card mt">
      <h3>Highlights</h3>
      <% f.fields_for :experience_highlights do |hf| %>
        <div class="highlight-row">
          <div>
            <%= hf.label :text, 'Highlight' %>
            <%= hf.text_field :text, class: 'input' %>
          </div>
          <div>
            <%= hf.label :sort_order %>
            <%= hf.number_field :sort_order, class: 'input', min: 0 %>
          </div>
          <div>
            <%= hf.label :_destroy, 'Remove' %>
            <%= hf.check_box :_destroy %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
=======
  </div>

  <div class="card mt">
    <h3>Skills</h3>
    <div class="skills-select" data-skills-select>
      <details>
        <summary data-skills-summary>Select skills</summary>
        <div class="skills-select-options">
          <%= f.collection_check_boxes :skill_ids, Skill.all, :id, :name do |cb| %>
            <label class="skills-select-option">
              <%= cb.check_box %> <%= cb.text %>
            </label>
          <% end %>
        </div>
      </details>
      <div class="skills-select-chips" data-skills-chips></div>
    </div>
  </div>

  <div class="card mt">
    <h3>Highlights</h3>
    <div id="highlight-rows">
      <%= f.fields_for :experience_highlights do |hf| %>
        <%= render 'highlight_fields', hf: hf %>
      <% end %>
    </div>

    <button type="button" class="secondary mt add-fields-btn" data-fields-container="highlight-rows" data-fields-template="highlight-template">+ Add another highlight</button>

    <template id="highlight-template">
      <%= f.fields_for :experience_highlights, ExperienceHighlight.new, child_index: 'NEW_RECORD_INDEX' do |hf| %>
        <%= render 'highlight_fields', hf: hf %>
      <% end %>
    </template>
  </div>
>>>>>>> origin/develop

  <div class="form-actions">
    <%= f.submit(@experience.persisted? ? 'Save experience' : 'Create experience', class: 'primary') %>
    <%= link_to 'Cancel', @experience.persisted? ? admin_experience_path(@experience) : admin_experiences_path, class: 'secondary' %>
  </div>
<% end %>
